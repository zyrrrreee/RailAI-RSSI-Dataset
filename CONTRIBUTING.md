# Contributing

## 基本规则

1. 不直接修改已发布版本的数据文件；
2. 新增或修改参数时，说明物理含义、单位、取值来源和敏感性；
3. 生成器修改必须更新版本号、配置哈希和验证结果；
4. 数据划分以 `run_id` 或更高层级分组，不得逐观测行随机切分；
5. 不提交缓存、日志、模型二进制、完整本地数据和含个人信息的文件；
6. Pull Request 中说明“当前实现、问题、修改方法、理论依据、验证结果”。

## 推荐分支

- `main`：可复现、通过检查的稳定草稿；
- `feature/<topic>`：单一功能开发；
- `docs/<topic>`：文档或数据字典；
- `fix/<topic>`：错误修复。

