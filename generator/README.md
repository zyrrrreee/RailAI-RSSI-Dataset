# Generator baseline

本目录保存现阶段已经通过本地测试的 RSSI 仿真核心代码副本。复制的目的，是让数据集仓库能够独立追溯生成器版本；原完整 GUI 工程仍在内部开发目录中保留。

当前兼容生成器输出的是阶段性研究格式。后续将增加统一的 `Scenario → Run → Sample` 批量导出层，而不推倒重写底层传播、接收机和故障机理。

不要在这里提交：

- GUI 日志；
- 已训练模型；
- HI 基线二进制；
- 大型验证输出；
- Word 临时文件；
- `__pycache__` 或虚拟环境。

